# Jolt (fork)

See the original repository and documentation at [a16z/jolt](https://github.com/a16z/jolt/blob/main/README.md).

This fork adds `integer-check`, `json-parse`, and `json-query` examples to measure CPU-only multithreaded proving performance both natively and in the browser (via WASM).
